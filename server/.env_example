DB_USERNAME=postgres
DB_PASSWORD=changeme
DB_DATABASE=book-worm
DB_HOST=127.0.0.1
PORT=3000
ACCESS_TOKEN_SECRET=generate-a-random-secret
REFRESH_TOKEN_SECRET=generate-a-random-secret

JWT_SECRET=generate-a-random-secret
JWT_PASS=generate-a-random-secret
EMAIL_USER=
EMAIL_PASSWORD=

# HTTP email API — SMTP is often blocked on hosting networks, Resend sends
# over plain HTTPS instead. Get a key at resend.com.
RESEND_API_KEY=
RESEND_FROM='Mr Book Worm <onboarding@resend.dev>'

# Used to build links inside confirmation/reset emails.
CLIENT_URL=http://localhost:5173
